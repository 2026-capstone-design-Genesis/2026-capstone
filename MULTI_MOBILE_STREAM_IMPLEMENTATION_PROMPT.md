# 멀티 모바일 스트림 구현 프롬프트

아래 프롬프트를 구현 담당 에이전트 또는 개발자에게 그대로 전달한다.

---

현재 프로젝트의 단일 휴대폰 카메라 실시간 관제 기능을 최대 4대 모바일 카메라 멀티스트림 기능으로 확장해줘. 구현 전 반드시 `MULTI_MOBILE_STREAM_DESIGN.md`를 읽고 그 설계를 따른다. 기존 동작을 깨지 말고, 코드 변경과 검증까지 수행해줘.

## 현재 코드 기준

- 백엔드: `realtime_server.py`
- React UI: `realtime_frontend/src/main.jsx`, `realtime_frontend/src/styles.css`
- 서버 테스트: `tests/test_realtime.py`
- 프런트엔드 테스트: `realtime_frontend/tests/`
- 실행 및 사용자 문서: `REALTIME_GUIDE.md`

현재 서버는 활성 세션이 있으면 신규 세션을 막고, UI는 하나의 `session`과 하나의 영상 패널만 관리한다. 이를 카메라당 독립 세션 방식으로 바꾼다.

## 반드시 구현할 요구사항

1. 활성 세션 제한
   - 동시에 활성인 세션을 최대 4개 허용한다.
   - 5번째 활성 세션 생성 요청은 HTTP 429를 반환한다.
   - 종료된 세션은 새 카메라 슬롯을 막지 않는다.
   - 기존 세션 ID·QR URL·phone/viewer WebSocket 경로는 유지한다.

2. 카메라별 세션
   - 카메라 한 대는 세션 하나와 연결한다.
   - 한 세션에 두 휴대폰이 붙지 못하게 하는 기존 보안 규칙은 유지한다.
   - 세션에 표시용 `label`과 `slot`을 추가한다. `label`이 생략되면 `카메라 01`처럼 안전한 기본값을 지정한다.
   - `snapshot()`과 세션 생성 응답에 `label`, `slot`을 포함한다.
   - 라벨을 파일 경로나 토큰에 사용하지 않는다.

3. 공용 분석 큐
   - 4개 세션이 동시에 분석 모델을 실행하지 않게 앱 전역 `asyncio.Queue`와 worker 1개를 사용한다.
   - 분석 요청은 큐에 쌓이고, UI snapshot에서 `waiting`, `running`, `idle`, `error` 상태를 구분할 수 있어야 한다.
   - 수신 및 viewer 프레임 전달은 분석 대기/실행 중에도 멈추면 안 된다.
   - 종료된 세션의 아직 실행되지 않은 작업은 안전하게 무시 또는 취소한다.
   - 기존 단일 세션 결과 파일 형식과 API는 호환성을 유지한다.

4. 다중 QR/관제 UI
   - 단일 `session` 상태를 카메라 목록 상태로 바꾼다.
   - 빈 슬롯에 세션을 생성하는 ‘카메라 추가’ UI를 제공하고, 각 카메라 카드에 QR, 라벨, 연결 상태, 링크 복사, 연결 종료를 제공한다.
   - 4대일 때 추가 기능을 비활성화하고 안내한다.
   - 각 카메라는 독립적인 `/ws/viewer/{session_id}` 구독과 Blob URL 정리를 수행한다.
   - 관제 영상은 연결 수에 따라 자동 분할한다: 1대는 전체 화면, 2대는 1×2, 3대와 4대는 2×2이다.
   - 각 타일에는 라벨, 번호, live/waiting/reconnecting/ended 상태, 최신 프레임, 수신 시간, 전체화면 버튼을 표시한다.
   - 프레임이 없는 타일은 빈 상태를 보이며 다른 타일에는 영향을 주지 않는다.
   - 타일을 선택하면 기존 키프레임 타임라인과 분석 패널이 그 카메라의 데이터로 전환된다.
   - 작은 화면에서는 그리드를 한 열로 바꾼다.

5. 성능·보안
   - JPEG 크기 검증, HTTPS 요구, same-origin 검사, 관리자 인증, QR 토큰 검증을 약화시키지 않는다.
   - 기본 전송 목표는 카메라당 긴 변 720px 이하, 2~4fps이다. 기존 전송 동작을 유지하거나 이 범위 안에서 명시적으로 제한한다.
   - 렌더링되지 않는 Blob URL, WebSocket, 타이머는 컴포넌트 정리 시 반드시 해제한다.

6. 테스트와 문서
   - `tests/test_realtime.py`에 다중 활성 세션, 5번째 세션의 429, 세션별 독립 전화 연결, 종료 후 슬롯 재사용, 분석 큐 동작 테스트를 추가 또는 수정한다.
   - 가능한 범위에서 Playwright 테스트에 1/2/3/4대 그리드 전환을 추가한다.
   - `REALTIME_GUIDE.md`를 최대 4대 연결 절차, QR이 카메라별로 다르다는 점, 권장 Wi-Fi/성능 설정으로 갱신한다.

## 작업 원칙

- 관련 없는 리팩터링, 의존성 교체, 기존 API 경로 변경은 하지 않는다.
- UTF-8을 보존하고, 기존 한글 문자열이 깨지지 않게 한다.
- 기존 단일 카메라 UI와 API를 사용하는 사용자가 계속 사용할 수 있어야 한다.
- 실제 코드베이스를 먼저 읽고 현재 구조에 맞춰 최소 변경으로 구현한다.
- 단위 테스트와 프런트엔드 빌드를 실행해 검증한다. 실패하면 원인을 고치거나, 외부 환경 문제라면 정확한 원인을 보고한다.

## 완료 보고 형식

완료 시 아래를 간결하게 보고해줘.

1. 바뀐 기능과 사용자 경험
2. 수정한 파일 목록과 핵심 변경점
3. 실행한 테스트·빌드와 결과
4. 실제 휴대폰 2대/4대 검증 시 사용자가 확인해야 할 항목
5. 남아 있는 제한 또는 위험 요소

---
