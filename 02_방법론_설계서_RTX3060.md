# LiveCC 키프레임 주변 클립 설명 생성: RTX 3060용 수정 설계서

작성: 2026-09-12. 상태: 샘플링·NF4 커널·고정 리비전 다운로드 완료, 전체 모델 추론 검증 전.
사용자가 제공한 방법론 설계서를 현재 코드와 PC 조건에 맞춰 수정한 문서다.
이 문서는 실행 설정을 변경하거나 양자화가 성공했다고 보고하는 문서가 아니다.

## 1. 목적과 범위

기존 시스템이 선택한 키프레임 주변의 짧은 구간을 시간순으로 관찰하고
한국어 한 문장을 생성한다. 기존 FD, 대표 프레임 선택, 녹화 및 보관함은 유지한다.
모델은 chenjoya/LiveCC-7B-Instruct를 사용하며 재학습·파인튜닝하지 않는다.
명칭은 **공개 LiveCC 모델의 4비트 양자화를 이용한 클립 단위 설명 생성 응용**이다.
스트리밍 학습이나 프레임별 실시간 해설의 재현으로 표현하지 않는다.
이벤트 체크·태그 기능은 추가하지 않는다.

## 2. 현재 환경과 제약

| 항목 | 확인 결과 | 설계 영향 |
|---|---|---|
| OS | Windows | 네이티브 CUDA 패키지 호환성을 먼저 확인 |
| Python | 3.12.13 | 별도 모델 환경도 동일 버전을 우선 검토 |
| 기존 GPU 환경 PyTorch | 2.5.1+cu121 | 기존 환경을 무조건 업그레이드하지 않음 |
| GPU | RTX 3060, VRAM 12288 MiB | 원본 BF16 GPU 단독 로딩 제외 |
| 관측 GPU 여유 | 앞선 점검에서 11102 MiB | 변동값이며 실행 직전 재측정 |
| 시스템 RAM | 17,008,259,072 bytes, 약 15.84 GiB | CPU offload를 기본 대안으로 두지 않음 |
| 디스크 여유 | 앞선 점검에서 37,315,129,344 bytes | 다운로드·설치 전 재측정 |
| 모델 캐시 | 기본 HF 캐시에서 LiveCC 미발견 | 별도 환경·캐시 위치 지정 필요 |
| 디코더 | 실시간 환경에 av==12.0.0 설치 | PTS 기반 프레임 추출 가능 |

확인한 모델 리비전: `dbe7415c5d2024c3e2d7f898ecbc58fd9fc8da2f`.
공개 BF16 가중치 4개 파일은 총 16,582,831,184 bytes다.
4비트로 로딩해도 원본 체크포인트 다운로드가 자동으로 4분의 1이 되지는 않는다.
전체 파라미터 수에 0.5 byte를 곱한 값은 실제 VRAM 추정치가 아니다.
양자화되지 않는 모듈, 양자화 메타데이터, 활성값, KV 캐시, CUDA 작업 공간이 추가된다.

## 3. 기존 설계에서 달라지는 점

| 항목 | 기존 제안 | 이 PC용 수정 |
|---|---|---|
| 가중치 로딩 | 원본 공개 모델 | bitsandbytes NF4 4비트 후보 |
| 계산 dtype | 원본 설정 | BF16 지원 확인 후 BF16; FP16은 명시적 별도 프로필 |
| attention | 공식 예제 FlashAttention 2 | SDPA를 Windows 호환성 실험으로 검증 |
| Liger | 공식 예제 적용 | 미적용 실험은 원전과 차이로 기록하고 품질 검증 |
| 동시 실행 | 미정 | GPU 모델 worker 1개, 추론 동시성 1 |
| CPU offload | 미정 | 기본 비활성화; RAM 실측 없이 자동 전환 금지 |
| API 대체 | 미정 | 기존 OpenAI 모드는 별도 선택; 실패 시 몰래 대체 금지 |
| 완료 기준 | 일반 품질 검증 | 로딩·메모리·입력 일치 검증 후 도메인 평가 |

bitsandbytes 문서의 Windows·NF4 지원은 LiveCC 전체 조합의 성공 보장이 아니다.
Qwen2-VL의 SDPA 지원도 양자화 LiveCC의 공식 경로와 수치적 동등함을 보장하지 않는다.
공식 예제는 Liger 적용을 강조한다. 이를 생략하는 Windows 경로는 명시적 변형이다.
실패하면 원인을 구분하고 Linux/외부 GPU에서 공식 경로를 검증한다.
WSL 설치를 자동으로 해결책으로 간주하지 않는다. 현재 RAM·디스크 제약도 함께 평가한다.

## 4. 실행 전 통과 조건

1. 기존 실시간 환경과 분리된 모델 환경을 만든다. 기존 torch를 교체하지 않는다.
2. 실제 설치 가능한 torch/CUDA/transformers/accelerate/bitsandbytes 조합을 확인한다.
   공식 모델은 Qwen2-VL 계열이다. 최신 버전을 무조건 선택하지 않고
   모델 import, 프로세서 입력, 양자화 kernel smoke test를 통과한 버전을 잠근다.
3. 모델 없이 작은 tensor로 CUDA와 NF4 연산을 먼저 시험한다.
4. 모델 원본 16.6GB, 패키지 설치, 다운로드 임시 공간, 녹화 보존 여유를 합산한다.
   RAM 가용량도 점검하고 캐시를 한 곳만 사용한다.
5. 지정 리비전의 파일만 다운로드한다. 학습용 optimizer 등 불필요한 파일은 제외한다.
6. `load_in_4bit=True`, `bnb_4bit_quant_type="nf4"`, double quantization 후보로 로딩한다.
   GPU에 배치된 실제 모듈과 양자화 제외 모듈을 기록한다. 자동 CPU 이동은 허용하지 않는다.
7. 2초·4장으로 한 번 추론하고 최고 VRAM, RAM, 로딩 시간, 추론 시간을 측정한다.
8. 화면 렌더링과 다른 GPU 사용량을 포함해 반복 실행 여유를 확인한 뒤 운영에 연결한다.

18 GiB 이상의 GPU를 요구하는 현재 코드의 BF16 사전 검사는 원본 프로필용이다.
이를 단순 삭제하지 않는다. 별도의 NF4 프로필과 실제 로딩 검사를 구현해야 한다.
성공 전에는 `.env`의 기본 OpenAI 설정을 유지한다.

## 5. 입력 구간과 시간 계약

입력: 영상, 기존 키프레임 시각 t, 모드, 구간 길이, 목표 프레임 수.
기본은 centered 2초·4장이다. 8장은 비교 설정이다.

- centered: [t-1, t+1]을 실제 영상 범위와 교차한다.
- past_only: [t-2, t]을 실제 영상 범위와 교차한다.
- 영상 범위를 벗어난 키프레임은 invalid_input으로 처리한다.
- 경계에서 부족한 시간을 반대쪽으로 늘리지 않는다.
- 진행 중인 영상에서 미래 구간이 아직 없으면 waiting이다. 실제 종료와 구별한다.
- 분석 주기별 저장 파일 끝은 카메라 영상의 실제 종료와 다르다.
  경계 주변 centered 입력은 다음 저장 파일에서 미래 프레임을 확보하거나 대기한다.
  현재 코드처럼 각 저장 파일을 무조건 ended=True로 처리하는 부분은 보완 대상이다.

PyAV의 실제 프레임 PTS를 기록한다. 수신 영상에 별도 capture timestamp가 있으면
인덱스와 매칭해 둘 다 저장한다. 파일 PTS와 세션 시각을 혼용하지 않는다.
영상에서 해당 구간 프레임을 찾을 수 없으면 no_frames로 처리한다.

## 6. 프레임 선택 및 모델 입력

1. 시간 범위 내의 실제 프레임을 대상으로 균등한 목표 시각을 구성한다.
2. 키프레임에 가장 가까운 유효 프레임을 포함한다.
3. 다른 후보를 교체할 때 양 끝의 관찰 범위가 불필요하게 줄지 않게 한다.
4. 같은 원본 프레임 중복을 제거하고 실제 시각순으로 정렬한다.
5. 짧은 영상은 관측 수를 그대로 기록한다. 패딩은 별도 필드에 기록한다.
6. 선택한 RGB 배열을 영상 텐서 하나로 전달한다. 경로를 다시 넘겨 재샘플링하지 않는다.
7. 실제 프로세서의 resize 및 temporal patch 처리를 기록하고 입력 동일성을 검사한다.

초기 해상도 상한 후보는 프레임당 78,400 pixels다. 고정 가로·세로 해상도가 아니다.
프로세서가 실제 적용한 높이·너비와 비디오 grid를 반드시 기록한다.
기본 입력 4장, max_new_tokens=160, do_sample=False, 추론 동시성=1을 후보로 둔다.
수치는 실험 초기값이며 품질이나 실행 성공을 보장하지 않는다.
OOM이면 프레임 수나 해상도를 자동으로 낮추지 않는다.

비균등한 간격을 Qwen2-VL 위치 입력에 정확히 전달할 수 있는지 별도 검증한다.
지원하지 않으면 시간순서만 전달했다고 기록하고 속도·정확한 지속 시간 설명은 금지한다.
오디오와 ASR은 입력하지 않는다. 구간 간 생성 문장·KV cache는 재사용하지 않는다.

## 7. 생성 프롬프트와 출력

> 제공된 영상 구간의 프레임을 시간순으로 살펴보고, 직접 확인되는 핵심 행동이나 상태 변화를 한국어 한 문장으로 설명하세요. 주체와 핵심 행동을 포함하고, 필요한 경우 대상이나 관찰된 변화를 덧붙이세요. 인물의 신원, 의도, 감정, 보이지 않는 원인이나 이후 결과를 추측하지 마세요. 뚜렷한 행동이 없으면 관찰되는 상태를 설명하세요. 설명을 위한 서문, 목록, 타임스탬프 없이 한 문장만 출력하세요. 불명확한 화면은 확인 가능한 수준으로 설명하고, 진행 중인 행동을 완료된 결과로 바꾸지 마세요.

출력 필드:

- keyframe_time_sec, clip_start_sec, clip_end_sec, clip_duration_sec
- frame_times_sec, decoded_pts, source_frame_indices
- observed_frame_count, padding_frame_count, model_input_frame_indices
- caption, raw_caption, retry_caption, status, error
- model_id, model_revision, quantization, compute_dtype, attention_backend, liger_enabled
- actual_dimensions, video_grid, library_versions
- observation_wait_seconds, queue_seconds, decode_seconds, load_seconds,
  preprocess_seconds, inference_seconds, postprocess_seconds, total_seconds
- peak_allocated_vram_bytes, peak_reserved_vram_bytes, process_ram_bytes

공백·명백한 접두어만 정리한다. 형식 오류는 최대 한 번 재요청하고 원문을 보존한다.
한국어 문장 검사는 형식 검사이지 사실 검증이 아니다. 빈 출력·여러 문장을
잘라서 정상 결과로 만들지 않는다. 별도 번역·캡션 요약 모델은 추가하지 않는다.
기존 전체 영상 요약 기능은 별도 단계로 유지하고 실패한 캡션은 요약 근거에서 제외한다.

상태는 ok, waiting, invalid_input, no_frames, decode_error, dependency_missing,
model_load_error, oom, inference_error, format_error로 구분한다.
모델 선택과 실제 사용 모델을 저장하고 추론 실패를 OpenAI 결과로 덮지 않는다.

## 8. 실시간 프로그램 연결

```text
실시간 수신 및 저장 → 기존 FD / 기존 대표 키프레임 선택
  → 원본 시각 기준 주변 클립 확보
  → 단일 모델 worker 대기열
  → NF4 LiveCC 클립 QA → 형식 검증
  → 해당 분석 회차에 캡션·입력 시각·오류 저장 → 현재 분석 / 보관함
```

모델은 최초 한 번만 로딩해 재사용한다. 웹 서버를 여러 개 띄워도 각 서버가
모델을 중복 로딩하지 않도록 별도 worker를 둔다. 캡션 추론은 녹화 수신을 막지 않는다.
입력이 처리 속도보다 빨리 쌓이면 대기열 길이와 pending 상태를 표시하고
파일 기반 작업 기록에 남긴다. 작업을 조용히 버리거나 실시간 완료로 표시하지 않는다.
현재 코드의 process-local lock만으로는 여러 서버 간 GPU 중복을 막을 수 없다.

키프레임 캡션은 해당 2초에 대한 설명이다. 기존 긴 행동 구간 쇼츠를 그대로 연결하면
설명 범위가 달라진다. UI에는 caption clip bounds와 전체 행동 구간을 구별하고
기본 재생은 캡션의 관찰 범위로 맞춘다. 긴 구간 연결로 캡션을 임의 확장하지 않는다.

## 9. 검증과 적용 순서

### 로직

키프레임 포함, 실제 PTS, 정렬·중복, 2초 미만, 시작·끝, 미래 프레임 미사용,
실시간 대기, 회차 경계, tensor 입력 일치, 패딩 구분, 오류 UI를 검증한다.
현재 샘플링 단위 테스트와 실제 WebM PTS 추출은 통과했다.
양자화 모델 tensor 입력 일치·추론 테스트가 통과한 것은 아니다.

### 성능 및 품질

같은 영상·키프레임으로 A:1장, B:2초 4장, C:2초 8장, D:긴 구간을 비교한다.
A를 이미지 입력으로 사용할지, 동일 프레임 패딩 영상으로 사용할지 구별해 기록한다.
프롬프트·해상도·양자화 프로필·리비전·생성 옵션을 고정한다.
정적 영상, 빠른 낙상, 가림, 다중 인물, 긴 동작을 포함한다.
주체·행동·대상·방향 오류, 없는 사건 추가, 한국어 형식 준수, 실패율을 평가한다.
원본 구간을 사람 평가자가 확인한다. 4비트에 따른 품질 저하는 미측정이다.
원본 BF16 비교는 더 큰 GPU에서 가능할 때 별도 수행한다.
초기 실행 성공만으로 실시간 운영 가능을 선언하지 않는다.

### 적용 단계

1. NF4 CUDA 연산과 설치 조합 검증.
2. 단일 2초 클립 로딩·추론·메모리 검증.
3. 1/4/8장 실제 CCTV 품질 비교.
4. 독립 worker·대기열·모델 실패 표시 연결.
5. 캡션 범위와 쇼츠 동기화, 회차 경계 처리.
6. 장시간 녹화와 동시 운용 테스트 후 backend 변경.

## 10. 재현과 보고

모델 캐시는 프로젝트 내부의 지정된 한 위치를 사용하고 가중치를 Git에 넣지 않는다.
성공한 환경은 pip freeze, Python/OS, 드라이버, torch CUDA runtime, 모델 SHA,
양자화 대상 모듈, 실제 입력 및 추론 옵션을 함께 저장한다.
라이브러리 버전은 아직 성공 조합을 측정하지 않았으므로 확정값을 만들어 넣지 않는다.

현재 보고 문구:
"기존 키프레임 주변의 시간순 프레임을 이용하는 LiveCC 클립 QA와 독립 작업 큐를
구현했다. RTX 3060 12GB에서 NF4 CUDA 커널과 고정 리비전 파일 검증은 통과했지만,
첫 전체 모델 로딩 시 Windows CUDA 프로세스가 종료 대기 상태로 남아 전체 로딩,
한국어 정확도와 실시간 지연은 아직 검증하지 못했다."

## 11. 근거

- LiveCC 공식 QA 및 Liger/FlashAttention 예제:
  https://github.com/showlab/livecc/blob/main/inference.md
- 공개 모델:
  https://huggingface.co/chenjoya/LiveCC-7B-Instruct
- 원전: https://arxiv.org/abs/2504.16030
  원문 제공 설계의 논문 설명은 배경이며 이 PC에서 측정한 결과가 아니다.
- bitsandbytes Windows/NF4 및 설정:
  https://huggingface.co/docs/transformers/v4.57.0/quantization/bitsandbytes
- Qwen2-VL attention 및 입력:
  https://huggingface.co/docs/transformers/main/model_doc/qwen2_vl

2초·4장·한국어 한 문장·NF4·SDPA는 응용 실험 조건이다.
논문 재현, 원본과 동일 정확도, 12GB 실행 보장, 최적 구간 길이를 주장하지 않는다.
