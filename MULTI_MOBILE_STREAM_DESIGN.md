# 최대 4대 모바일 카메라 멀티스트림 설계

## 1. 목적과 범위

기존의 단일 휴대폰 카메라 실시간 수신·분석 기능을 확장하여, 최대 4대의 모바일 카메라 영상을 관제 화면에서 동시에 확인한다. 연결된 카메라 수에 따라 화면은 자동으로 분할한다.

| 연결 대수 | 표시 레이아웃 |
| --- | --- |
| 0대 | 카메라 추가 안내 |
| 1대 | 단일 전체 화면 |
| 2대 | 1행 2열 |
| 3대 | 2행 2열 중 3칸 |
| 4대 | 2행 2열 |

이번 범위에는 모바일 브라우저 카메라, QR 페어링, JPEG-over-WebSocket 수신, 다중 미리보기, 카메라별 연결 상태 및 카메라별 분석 결과 저장을 포함한다. 5대 이상, 음성 수집, 외부 인터넷 공개 중계, WebRTC 전환은 범위에서 제외한다.

## 2. 현 구조와 변경 이유

현재 서버는 활성 세션이 하나라도 있으면 새 세션 생성을 거부하고, 각 세션은 하나의 휴대폰 소켓과 하나의 최신 JPEG 프레임만 가진다. React 관제 화면 또한 하나의 `session` 상태와 하나의 `useCameraSession` 구독만 사용한다.

따라서 다중 연결은 한 세션에 여러 휴대폰을 억지로 붙이기보다, **카메라 한 대당 독립 세션 하나**를 유지하는 방식으로 확장한다. 기존 저장·분석·재연결 단위를 유지할 수 있고, 한 카메라의 장애가 다른 카메라에 영향을 주지 않는다.

## 3. 목표 아키텍처

```text
휴대폰 A ─ WebSocket /ws/phone/{sessionA} ─┐
휴대폰 B ─ WebSocket /ws/phone/{sessionB} ─┼─ FastAPI 세션 관리자
휴대폰 C ─ WebSocket /ws/phone/{sessionC} ─┤       ├─ 수신/녹화/분석 상태 (카메라별)
휴대폰 D ─ WebSocket /ws/phone/{sessionD} ─┘       └─ 분석 작업 큐 (공유)
                                                     │
관제 React ─ /ws/viewer/{sessionA..D} ─ 멀티 카메라 그리드
```

- `Session`은 카메라 하나의 입력, 최신 프레임, 녹화, 키프레임, 결과 디렉터리를 계속 소유한다.
- 서버의 `sessions` 딕셔너리는 동시에 활성인 세션을 최대 4개 보관한다.
- 각 생성 응답은 기존처럼 해당 카메라에만 유효한 `phone_url`과 토큰을 반환한다.
- 관제 화면은 카메라 카드 목록을 관리하며, 카드마다 viewer WebSocket을 하나씩 구독한다.
- 비싼 분석 작업은 공용 큐를 통해 한 번에 하나만 실행한다. 프레임 수신과 미리보기는 분석 중에도 계속된다.

## 4. 백엔드 설계

### 4.1 세션 생성과 제한

- `POST /api/sessions`의 “활성 세션이 있으면 거부” 조건을 제거한다.
- 종료되지 않은 세션 수가 4개 이상이면 HTTP 429를 반환한다.
- 완료/중지된 세션은 신규 카메라 슬롯을 차지하지 않는다.
- 기존 URL, 토큰 검증, same-origin 검증, 세션 만료 정책은 유지한다.

### 4.2 카메라 식별 정보

세션 생성 요청에 선택 항목 `label`을 추가한다. 비어 있으면 서버가 `카메라 01`부터 현재 빈 번호를 기준으로 기본 이름을 부여한다.

`snapshot()`에는 아래 정보를 추가한다.

```json
{
  "id": "...",
  "label": "입구",
  "slot": 1,
  "status": "live",
  "received_frames": 42
}
```

라벨은 표시 목적이며, 파일 경로와 인증 토큰에 사용하지 않는다. 파일 저장은 현재처럼 세션 ID 기반 이름을 유지한다.

### 4.3 수신·미리보기

- `/ws/phone/{session_id}`의 한 세션당 한 휴대폰 연결 제약은 유지한다.
- `/ws/viewer/{session_id}`도 기존처럼 해당 세션의 최신 JPEG와 상태만 보낸다.
- viewer 최대 4개 제한은 **세션별 관제 브라우저 수** 제한으로 유지해도 된다. 한 관제 화면에서 세션 4개를 구독하는 것은 서로 다른 세션이므로 허용된다.
- 프레임 크기 제한과 JPEG 검증은 유지한다.

### 4.4 분석 부하 제어

4개 세션의 분석이 동시에 모델을 실행하지 않도록 앱 수명주기에 공유 `asyncio.Queue`와 worker 하나를 둔다.

- 세션이 분석 시점에 도달하면 바로 모델을 실행하지 않고 분석 요청을 큐에 넣는다.
- 큐 대기 상태는 `analysis_status: waiting`으로 snapshot에 노출한다.
- worker는 작업 하나를 끝낸 뒤 다음 작업을 실행한다.
- 세션 중지 후 아직 시작하지 않은 작업은 취소하거나 안전하게 건너뛴다.
- 단일 카메라 동작의 API 응답 형식과 결과 파일 형식은 호환성을 유지한다.

초기 운영값은 카메라당 긴 변 720px 이하, 2~4fps, JPEG 품질 0.62를 권장한다. 실제 GPU/CPU·Wi-Fi 상황을 확인한 뒤 올린다.

## 5. 프런트엔드 설계

### 5.1 상태 모델

단일 `session` 상태를 다음과 같은 목록으로 확장한다.

```js
const [cameras, setCameras] = useState([]);
// [{ id, label, slot, phone_url, snapshot, frame, online }]
```

`useCameraSession`은 카메라 ID 하나를 인자로 받아 그대로 재사용하거나, `CameraTile` 컴포넌트 안에서 호출한다. 각 타일은 자기 WebSocket과 Blob URL을 생성·해제해야 한다.

### 5.2 연결 화면

- ‘카메라 추가’ 버튼으로 빈 슬롯에 세션을 생성한다.
- 각 카메라 카드에 라벨, QR 코드, 연결 상태, 링크 복사, 연결 종료를 표시한다.
- 4대가 연결되면 추가 버튼을 비활성화하고 최대 연결 수를 안내한다.
- 종료된 카드의 세션은 목록에서 제거하거나 ‘종료됨’ 상태로 남긴 뒤 사용자가 제거하도록 한다. 권장 UX는 종료 후 제거 버튼을 노출하는 것이다.

### 5.3 관제 그리드

```text
1대: [             camera 1             ]
2대: [      camera 1      ][      camera 2      ]
3대: [      camera 1      ][      camera 2      ]
     [      camera 3      ][       empty        ]
4대: [      camera 1      ][      camera 2      ]
     [      camera 3      ][      camera 4      ]
```

- CSS Grid로 `camera-grid count-1`부터 `count-4` 클래스를 사용한다.
- 각 타일에는 라벨, 순번, live/waiting/reconnecting/ended 상태, 최신 프레임, 수신 시간, 전체화면 버튼을 표시한다.
- 최신 프레임이 없으면 빈 상태를 표시하며 다른 타일의 레이아웃은 유지한다.
- 타일 클릭 시 그 카메라의 키프레임·분석 패널을 선택한다. 기본 선택은 첫 live 카메라다.
- 작은 화면에서는 타일을 세로 1열로 배치한다.

## 6. API 호환성과 보안

- 기존 단일 세션 API 경로와 WebSocket 경로는 바꾸지 않는다.
- 기존 관리자 쿠키 인증, QR 토큰의 URL fragment 전달, HTTPS 요구, same-origin 검사, 세션 만료를 유지한다.
- `GET /api/config`의 `sessions` 배열을 활성 카메라 목록에 사용한다.
- 토큰은 관제 화면이나 로그에 표시하지 않으며 QR 링크는 기존과 동일하게 복사·공유 주의 문구를 둔다.

## 7. 구현 순서

1. 서버의 활성 세션 제한을 최대 4대로 변경하고 라벨·슬롯 정보를 추가한다.
2. 공유 분석 큐와 단일 worker를 추가한다.
3. 프런트엔드 상태를 카메라 배열로 전환하고 카메라 카드·QR 생성을 구현한다.
4. `CameraTile`과 반응형 1/2/3/4대 그리드를 구현한다.
5. 선택 카메라의 타임라인·분석 패널을 연결한다.
6. 서버 단위 테스트와 프런트엔드 흐름 테스트를 다중 세션 시나리오로 확장한다.
7. 실제 휴대폰 2대, 이후 4대로 Wi-Fi 및 분석 부하를 측정한다.

## 8. 완료 기준

- 최대 4개의 활성 QR 세션을 만들 수 있고, 다섯 번째 요청은 429가 된다.
- 서로 다른 휴대폰 2~4대가 각각 자기 QR로 연결된다.
- 연결 대수 변화에 따라 관제 화면이 1 / 2 / 2×2 레이아웃으로 즉시 전환된다.
- 한 카메라를 중지·재연결해도 다른 카메라의 영상 수신과 상태가 유지된다.
- 각 카메라의 출력 폴더와 분석 결과가 서로 섞이지 않는다.
- 분석 요청이 동시에 여러 개 들어와도 모델 실행은 한 번에 하나이며, 대기 상태가 UI에 표시된다.
- 기존 단일 카메라 테스트와 사용 흐름이 계속 동작한다.
